# Runs a demo app with the code

Runs a demo app with the code

## Usage

``` r
runDirinputExample()
```

## Value

NA

## Details

Runs a demo app with the code. See the code in the example_application
folder for how this is accomplished.

## Examples

``` r
if (interactive()) {
    shiny::runApp(runDirinputExample())
}
```
