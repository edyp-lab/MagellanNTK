# convert_dataset_ui and convert_dataset_server

A shiny Module.

## Usage

``` r
convert_dataset_ui(id)

convert_dataset_server(id)

convert_dataset()
```

## Arguments

- id:

  xxx

## Value

A list

## Examples

``` r
if (interactive()) {
    shiny::runApp(convert_dataset())
}
```
