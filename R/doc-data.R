#' @name lldata
#' @title lldata dataset
#' @description An instance of the class `MultiAssayExperiment` which contains
#' one assay (instance of class `SummarizedExperiment`):
#' 'Convert': Contains a data.frame with 6 columns and 100 rows fullfilled with '0'. 
#' It also contains a slot 'history' in the metadata(). 
#' This dataset is the most
#' bacis one to run the example pipeline and learn how to work with
#' MagellanNTK
#' @docType data
#' @keywords data
#' @usage data(lldata)
#'
#' @format An object of class `MultiAssayExperiments`
#'
#' @keywords datasets
NULL



#' @name lldata1
#' @title lldata1 dataset
#' @description An instance of the class `MultiAssayExperiment` which contains
#' two assays (instance of class `SummarizedExperiment`):
#' 'Convert': Contains a data.frame with 6 columns and 100 rows fullfilled with '0'. 
#' 'DataGeneration' which stores the result of the 'DataGeneration' step of the 
#' demo pipeline
#' Each assay contains a slot 'history' in the metadata(). 
#' @docType data
#' @keywords data
#' @usage data(lldata1)
#'
#' @format An object of class `MultiAssayExperiments`
#'
#' @keywords datasets
NULL



#' @name lldata12
#' @title lldata12 dataset
#' @description An instance of the class `MultiAssayExperiment` which contains
#' two assays (instance of class `SummarizedExperiment`):
#' 'Convert': Contains a data.frame with 6 columns and 100 rows fullfilled with '0'. 
#' 'DataGeneration' which stores the result of the 'DataGeneration' step of the 
#' demo pipeline
#' 'Preprocessing' which stores the result of the 'DataGeneration' and 'Preprocessing'
#' steps of the demo pipeline
#' Each assay contains a slot 'history' in the metadata(). 
#' @docType data
#' @keywords data
#' @usage data(lldata12)
#'
#' @format An object of class `MultiAssayExperiments`
#'
#' @keywords datasets
NULL



#' @name lldata123
#' @title lldata123 dataset
#' @description An instance of the class `MultiAssayExperiment` which contains
#' two assays (instance of class `SummarizedExperiment`):
#' 'Convert': Contains a data.frame with 6 columns and 100 rows fullfilled with '0'. 
#' 'DataGeneration' which stores the result of the 'DataGeneration' step of the 
#' demo pipeline
#' 'Preprocessing' which stores the result of the 'DataGeneration' and 'Preprocessing'
#' steps of the demo pipeline
#' 'Clustering' which stores the result of all the three steps of the demo pipeline
#' Each assay contains a slot 'history' in the metadata(). 
#' @docType data
#' @keywords data
#' @usage data(lldata123)
#'
#' @format An object of class `MultiAssayExperiments`
#'
#' @keywords datasets
NULL
